---
interface Props{
    link: string;
}

const { link } = Astro.props;
---
<a href={link} class="flex flex-row gap-x-2 hover:bg-personal-gray/30 personal-shadow-sm border-2 border-personal-black rounded-full items-center justify-center w-fit px-4 py-2 text-base max-sm:text-xs">
    <slot />
</a>