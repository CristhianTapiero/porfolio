---
import { Icon } from "astro-icon/components"

const social = [
    {
        name: 'Instagram',
        url: 'https://www.instagram.com/tapieropadilla/',
        icon: "instagram"
    },
    {
        name: 'X',
        url: 'https://twitter.com/tapiero_padilla',
        icon: "x"
    }
]
---

<div class="absolute bottom-0 left-0 flex items-center justify-evenly h-16 w-full px-4 py-2 bg-brand-gray text-brand-white">
    <p class="text-base max-sm:text-xs">&copy; Unico y detergente, gracias por llegar hasta aquí.</p>
    <div class="flex gap-x-2">
        {
            social.map(item => (
                    <a href={item.url} class="flex cursor-pointer "><Icon name={`simple-icons:${item.icon}`} class='size-4' /></a>
                )
            )
        }
    </div>
</div>